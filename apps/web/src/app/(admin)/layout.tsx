// Route-group layout — transparent wrapper.
// Admin pages live in app/admin/ (not here).
export default function AdminGroupLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
